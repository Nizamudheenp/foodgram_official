
const isLocalhost = ['localhost', '127.0.0.1'].includes(window.location.hostname);

const BASE_URL = isLocalhost
  ? 'http://localhost:3000'
  : 'https://foodgramofficial-production-74e5.up.railway.app';

